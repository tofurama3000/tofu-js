/**
 * @module is:isArray
 * @ignore
 */

/**
 * Returns whether or not something is an array
 * @kind function
 * @param {any} obj the object to test
 * @returns {boolean} Whether or not it is an array
 */
export const isArray = Array.isArray;
