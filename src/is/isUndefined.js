/**
 * @module is:isUndefined
 * @ignore
 */

/**
 * Returns whether or not something is undefined
 * @kind function
 * @param {any} obj the object to test
 * @returns {boolean} Whether or not it is undefined
 */
export const isUndefined = param => typeof param === 'undefined';
