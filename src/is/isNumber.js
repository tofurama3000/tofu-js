/**
 * @module is:isNumber
 * @ignore
 */

/**
 * Returns whether or not something is a number
 * @kind function
 * @param {any} obj the object to test
 * @returns {boolean} Whether or not it is a number
 */
export const isNumber = param => typeof param === 'number';
